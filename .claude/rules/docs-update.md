# Rule: Documentation Updates
## When: Data model, API, business rules, or deploy logic changes
## Do:
- Update relevant docs/ file when feature changes
- Keep docs/api-spec.md in sync with Server Actions
- Keep docs/business-rules.md in sync with lifecycle rules
## Don't:
- Don't skip docs updates when contracts require them
## Why: Docs reflect the current state of the system.
